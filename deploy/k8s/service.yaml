apiVersion: v1
kind: Service
metadata:
  name: ledgerline
  namespace: ledgerline
  labels:
    app.kubernetes.io/name: ledgerline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ledgerline
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
